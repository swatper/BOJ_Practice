#include <iostream>
using namespace std;

int main() {
	cin.tie(NULL);
	cout.tie(NULL);
	ios_base::sync_with_stdio(false);
	int Tc;
	cin >> Tc;
	for (int i = 1; i <= Tc; i++)
	{
		cout << "Hello World, Judge " << i << "!\n";
	}
	return 0;
}