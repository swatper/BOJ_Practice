#include <iostream>
using namespace std;

int main() {
	cin.tie(NULL);
	cout.tie(NULL);
	ios_base::sync_with_stdio(false);
	char cha;
	cin >> cha;
	//강제 형 변환
	cout << (int)cha << "\n";
	return 0;
}